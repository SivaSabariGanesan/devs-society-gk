# Supabase Configuration Template
# Copy this file to .env and fill in your actual values

SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# JWT Secret (generate a secure random string)
JWT_SECRET=your_jwt_secret_key

# Server Configuration
PORT=5050
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Instructions:
# 1. Copy this file to .env: cp supabase.env.example .env
# 2. Go to your Supabase project dashboard
# 3. Navigate to Settings > API
# 4. Copy the Project URL to SUPABASE_URL
# 5. Copy the anon public key to SUPABASE_ANON_KEY
# 6. Generate a secure JWT_SECRET (you can use: openssl rand -base64 32) 